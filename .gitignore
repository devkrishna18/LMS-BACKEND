# Dependencies
node_modules/
package-lock.json   # Optional: Keep this if you want to lock versions, ignore if using yarn exclusively
yarn.lock           # Optional: Keep this if using yarn
pnpm-lock.yaml      # Optional: Keep this if using pnpm

# Environment Variables (CRITICAL: Do not remove this line)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log
pids
*.pid
*.seed
*.pid.lock

# Operating System files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Text Editor / IDE files
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Build output (if you ever add a frontend build to this folder later)
dist/
build/
coverage/

# Razorpay / Payment signatures (just in case)
razorpay_signatures/